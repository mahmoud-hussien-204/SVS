export enum EnumAppKeys {
  isLoggedIn = "isLoggedIn",
  userData = "userData",
  userJourney = "userJourney",
}

export enum EnumUserRole {
  admin = "admin",
  user = "user",
}

export enum EnumUsersRoutes {
  admin = "/admin",
  user = "/user",
}

export enum EnumUserStatus {
  active = "active",
  inactive = "inactive",
  pending = "pending",
}

export enum EnumBuyCoinStatus {
  banned = "banned",
  failed = "failed",
  completed = "completed",
  active = "active",
  inactive = "inactive",
  pending = "pending",
}

export enum EnumCoinHistoryStatus {
  accepted = "accepted",
  rejected = "rejected",
  failed = "failed",
  pending = "pending",
}

export enum EnumModals {
  success = "success",
  error = "error",
  warning = "warning",
  info = "info",
  edit = "edit",
  add = "add",
  view = "view",
  delete = "delete",
  filter = "filter",
  status = "status",
  confirmation = "confirmation",
  withdraw = "withdraw",
  deposit = "deposit",
  swap = "swap",
  transfer = "transfer",
  invite = "invite",
  enableTwoFactorAuthentication = "enableTwoFactorAuthentication",
  updatePassword = "updatePassword",
  suspended = "suspended",
}
